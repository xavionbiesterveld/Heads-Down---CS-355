
body {
    max-width: 100%;
    max-height: 100%;
    overflow: hidden;
    text-align: center;
    font-family: Arial, sans-serif;
    background-image: url(home-background.jpg);
    background-color: #84a2d4;
    background-repeat: no-repeat;
    background-size: cover;
    background-position-x: center;
}

.container {
    display: grid;
    grid-template-rows: 80px auto 1fr auto 50px;
    grid-template-areas: 
        "header"
        "banner"
        "main"
        "low-content"
        "footer";
    grid-gap: 10px;
    padding: 10px;
    font-weight: 600;
    font-size: 20px;
}

/* Game Area */
#game-instructions, #game-area, #players {
    margin: 20px auto;
    width: 80%;
    text-align: center;
}

/* Players */
#players {
    margin-top: 20px;
}

#player-list {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
}

.player {
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: #e6f7ff;
    padding: 10px;
    border: 2px solid #0073e6;
    border-radius: 10px;
    width: 120px;
}

.player img {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background-color: #0073e6;
}

.player-name {
    margin: 10px 0;
    font-weight: bold;
}

.player-score {
    font-size: 1.2rem;
    margin: 5px 0;
}

.player-buttons {
    display: flex;
    gap: 5px;
}

.player-buttons button {
    padding: 5px;
    font-size: 1rem;
    background-color: #0073e6;
    color: white;
    border: none;
    cursor: pointer;
    border-radius: 5px;
}

.player-buttons button:hover {
    background-color: #005bb5;
}

/* Popup Forms */
.wrapper {
    /* Existing styles */
}

/* More styles for wrapper and forms here */
